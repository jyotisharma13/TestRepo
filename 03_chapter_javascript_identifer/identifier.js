var name = "jyoti";

var firstName = "jyoti";
var last_name = "Sharma"; // snake_case
var This_is_a_very_long_name_variable = "jyoti";
var lastName = "sharma"; // CamelCase

// Naming Conventions (Cases)
// ============================================
// 1. camelCase (standard for JS variables and functions)
let userName = "camelCase";
let totalPrice = 99.99;
let isLoggedIn = true;
let lastLoginDate = new Date(2026, 7, 11); // Note: Months are 0-indexed in JavaScript

// 2. PascalCase (standard for JS classes and constructors)
let UserProfile = "PascalCase";
let ShoppingCart = "class name style";
let OrderHistory = "PascalCase for classes";


// 3. snake_case (underscore separated)
let user_name = "snake_case";
let total_price = 45.99;
let is_logged_in = false;


// 4. SCREAMING_SNAKE_CASE (constants)
const MAX_SIZE = 100;
const API_KEY = "abc123";
const DATABASE_URL = "localhost";


// 5. Hungarian Notation (prefix with type - older style)
let strName = "string prefix";
let bActive = true;       // boolean
let nCount = 5;           // number
let arrItems = [];        // array
let amtBilling = 99.99;    // amount (number)
