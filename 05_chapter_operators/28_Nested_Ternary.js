let age = 17;
// age>= 18 -> he will go to goa
//drink <25 yes else no
let isAdult = age >= 18 ? "Yes" : "No";
let canDrink = age > 25 ? "Yes" : "No";
console.log(`Is the person an adult? ${isAdult} also can he drink? ${canDrink}`);

let is_shivi_enjoying = age >= 18 ? (age > 25 ? "Yes" : "No") : "No";
console.log(`Is Shivi enjoying the trip? ${is_shivi_enjoying}`);